general {
    lock_cmd = hyprlock
    before_sleep_cmd = hyprlock
    after_sleep_cmd = sleep 1
    inhibit_sleep = 3
}

listener {
    timeout = 1500
    on-timeout = hyprlock
}

listener {
    timeout = 1980
    on-timeout = hyprlock
}
